---
apiVersion: v1
kind: Service
metadata:
  name: lasntg-service
  labels:
    app: lasntg-service
spec:
  selector:
    app: lasntg
  ports:
  - name: http
    protocol: TCP
    port: 8080
    targetPort: 80
